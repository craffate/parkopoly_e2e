module.exports = function(Parent) {
  this.driverInput = $('input[placeholder="Drivers"]');
  this.driverSalaryInput = element(by.id('driver-salary'));
  this.driverConcessionCost = element(by.id('concession-cost'));
  this.submitButton = $('button[type="submit"]');
};

module.exports = step6;
